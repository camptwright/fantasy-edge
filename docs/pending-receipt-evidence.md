# Receipt needed for the ledger's next verification step

Provide one redacted receipt or native export from the preferred bookmaker.
Retain: bookmaker and product name; receipt/bet ID; game and scheduled date;
player/selection, market, period, side and exact line; American or decimal odds
with format labeled; stake and currency; placement timestamp and timezone;
receipt jurisdiction if supplied; and any market-specific settlement clauses.
For a settled receipt, include status, returned amount (whether stake is included),
settled timestamp, and correction/cashout/bonus indicators.

Remove account numbers, real account identifiers, payment details, credentials,
addresses and balances. Use a consistent local account nickname instead.

A receipt validates a native import adapter and placement identity. It does NOT
prove official closing price. Verified CLV additionally needs a dated official
closing observation for the identical contract, plus archived applicable rules,
their effective interval, and independently reviewed product/jurisdiction scope.
No receipt or official-close evidence has been supplied yet. Existing JSON imports
are normalized claims; existing unmatched closing observations remain proxies.
